服务器端在窗口创建时，创建一个新的线程用于创建服务器端套接字，并监听客户端的连接，对应与接受到的每一个客户端连接，再创建新的线程响应客户端的请求，把服务端的屏幕数据传送。另外，接受客户端的鼠标与键盘操作的命令，在服务器端触发相应的鼠标与键盘事件。
客户端与服务器端的交互正好与之相反，连接服务器，发送请求屏幕数据命令，并接受服务器端屏幕数据在客户端显示。另外，处理用户的键盘和鼠标事件，并把对应的数据发送给服务器端。

